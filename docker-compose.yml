services:
  web:
    build: .
    container_name: wa_quiz_web
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: wa_quiz_redis
    ports:
      - "6379:6379"